import React, { Component } from "react";

class Register extends Component {
  render() {
    return (
      <section className="homeSection">
        <div className="homeDiv">Maak een Account aan gappies!</div>
      </section>
    );
  }
}

export default Register;
